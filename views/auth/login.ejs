<%- include('../includes/head.ejs') %>

<link rel="stylesheet" href= "/css/forms.css"/>
<link rel="stylesheet" href="/css/auth.css"/>
    </head>

    <body>
        <%- include('../includes/navigation.ejs') %>
        <main>
          <% if (errorMessage) {  %>
           
            <div class="user-message user-message--error"> 🤦‍♂️🤦‍♂️🤦‍♂️<%= errorMessage%></div>
            <% } %>
            <form action="/login" method="POST" class="login-form" novalidate>
              <input type="hidden" name="_csrf" value="<%= csrfToken%>" />
                <div class="form-control">
                  <label for="email">Email-ID</label>
                  <input class="<%= validationErrors.find(e => e.param === 'email')? 'invalid':''%>" type="email" name="email" id="email" placeholder="Enter your Email ID" value="<%=oldInput.email%>" />
                </div>
                <div class="form-control">
                  <label for="password" >Password</label>
                  <input class="<%= validationErrors.find(e => e.param === 'password')? 'invalid':''%>" type="password" name="password" id="password" placeholder="Enter thy Password" value="<%=oldInput.password%>"/>
                </div>
                <button type="submit" class="btn">Log-In</button>
              </form>
              <div class="centered">
                <a href="/reset"> Reset Password</a>
              </div>
        </main>
        <%- include('../includes/end.ejs') %>